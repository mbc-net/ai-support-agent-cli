---
# dns_tls ロールの既定値。

# ACME（Let's Encrypt）へ登録する連絡先メールアドレス。
#
# 未設定でも証明書は取得できるが、**Let's Encrypt からの有効期限切れ予告メールが
# 誰にも届かない**。Caddy の自動更新が継続的に失敗しても、失効するまで気づく手段が
# 無くなるため、実運用では必ず設定すること。
#
# 空文字のときは Caddyfile にグローバルオプションを書かない（Caddy 既定の挙動＝
# 連絡先なしでの登録のまま）。既に稼働しているホストの挙動を変えないための既定である。
acme_email: ''
